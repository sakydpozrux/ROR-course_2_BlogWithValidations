%h1 All microposts:

%h2=button_to 'Create one', new_micropost_path, method: :get

%table
  %tr
    %th Title
    %th Content
    %th Created
    %th
    %th
    %th

  - @microposts.each do |micropost|
    %tr
      %td= micropost.title
      %td= micropost.content
      %td= micropost.created_at
      %td= link_to 'Show', micropost
      %td= link_to 'Edit', edit_micropost_path(micropost)
      %td= link_to 'Remove', micropost_path(micropost), method: :delete, data: { confirm: 'Are you sure?' }
